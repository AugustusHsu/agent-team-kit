# agent-team-kit 出貨的基礎 .gitignore
# 這是「最低必要」而非完整清單——請依專案技術棧自行往下加。

# --- Worktree（必要，不可移除）---
# worktree 目錄內含一個 .git 檔案，若被 git add 會變成詭異的 gitlink。
# 即使專案不採用 worktree 流程，Claude Code 仍可能建立，故保留本規則。
.claude/worktrees/

# --- 多代理本機狀態（共同政策可版控，本機狀態不可）---
.agent/agent-runtime.local.json

# Codex 專案設定採 allowlist：portable config／actions 可提交，個人 hook 與暫存狀態不提交
.codex/*
!.codex/config.toml
!.codex/actions/
!.codex/actions/**

# --- 接手文件 ---
# 每次對話的暫時狀態，不進版控
HANDOFF.md

# --- 機密 ---
.env
.env.*
!.env.example
*.pem

# --- 本套件腳本的產生物 ---
__pycache__/
*.py[cod]

# --- OS / 編輯器 ---
.DS_Store
*.swp
