# 審查紀錄：PEV-DEV-AGENT-023

**工單**：[取證通道保真](../tasks/PEV-DEV-AGENT-023.md)
**審查者**：code-reviewer
**日期**：2026-08-18
**結論**：✅ **APPROVED**

> ⚠️ **本輪開發與審查發生在同一個 context**（§2.3 明列的情況）。因此本檔刻意把
> 「機器可複核」與「僅人工判讀」分開列——§1 每一條都附可重跑的指令或檔案:行數，
> §5 集中列出無法客觀驗證的部分，不偽裝成已驗證。

## 1. AC 逐條核對

| AC | 結果 | 證據（可重跑） |
|---|---|---|
| AC-01 §1.12 新增、全角色、無產品名 | ✅ | `team_protocol.md:230` 起；`grep -rli 'headroom\|ccr_store\|kompress' kit/ \| wc -l` → **0** |
| AC-02 技術棧無關探針＋判準 | ✅ | 探針只要求「shell 能印五行字」，無語言／框架依賴；判準含「設定有寫進去不算數，探針通過才算數」 |
| AC-03 兩種失效各給處置 | ✅ | §1.12 的失效對照表：有標記 → 換讀法重讀；無標記 → 只能靠事前自檢 |
| AC-04 §2.3 收斂成指路 | ✅ | 原 6 行 bullet → 3 行，正版只留 §1.12 一份 |
| AC-05 code-reviewer §0 改指 §1.12 | ✅ | 原 7 行 → 3 行；`test_指路檔不得抄襲正版內容` 綠 |
| AC-06 四支 SKILL 各補一行 | ✅ | backend §3、frontend §2、devops §3（皆掛在「防幻覺機制」底下）、qa-automation §9.1 步驟 6 |
| AC-07 指路檔索引補 §1.12 | ✅ | `test_指路檔章節索引與正版同步` 綠；負向對照見 §3 |
| AC-08 不進 `precheck.py` | ✅ | `precheck.py` 零改動；理由寫進工單 §5 與 §1.12 末段 |
| AC-09 pytest 全綠、dry-run 相符 | ✅ | `135 passed in 7.08s`、`exit=0`；dry-run「更新 7、**待合併 0**」＝實際改動的 7 個 kit 檔案 |

## 2. 設計判斷：為什麼是 §1.12 而不是 §2.x

`017` 把正版放在 §2.3「Code Reviewer → Done／退回」底下，是**位置本身造成的漏接**：
交接規範只有交接雙方會讀，但取證是全角色每天在做的事。

§1 後段（§1.9 程式碼隔離、§1.10 Commit 閘門、§1.11 文檔權威階序）實際上已經是
**橫切全角色的紀律區**，不再是工單狀態機。§1.12 放在這裡與既有結構一致。

## 3. 負向對照（a）：拿掉就要紅

| 動作 | 期望 | 實測 |
|---|---|---|
| 從指路檔刪掉 `\| §1.12 \|` 那列 | 紅 | 🔴 `exit=1`，訊息 `指路檔的章節索引漏了：§1.12` |
| 在 `kit/` 塞回「這件事關不掉，只能靠讀法規避」 | 紅 | 🔴 `exit=1`，命中 `_neg_probe.md:1` |
| 移除該檔後重跑 | 綠 | ✅ `exit=0` |

兩項都確認**不是恆綠**。

## 4. 推翻的舊規範

`017` 留下的「這件事關不掉，只能靠讀法規避」已被根因追查推翻——中間層持有工具保護名單，
**保真是可設定的**，規避只是修不了時的退路。依 CLAUDE.md 的規定加進
`tests/test_kit_integrity.py` 的 `已廢除的流程規則` 表。

**曾考慮但撤回的一條規則**：原本另加了 `team_protocol\.md` §2\.3。$` 想抓「正版還指向 §2.3」
的殘留，`grep -rn '§2\.3' kit/` 後撤回——§2.3 仍是合法章節，
`docs/features/_TEMPLATE/reviews/README.md:10`、`git_workflow.md:123`／`:297`、
`docs/features/README.md:31` 都正當指向它（指的是審查紀錄落點規則），
**沒有能區分新舊語義的正則**，硬加會製造假紅燈。這條取捨記在測試檔的註解裡。

## 5. 僅人工判讀（無法客觀驗證）

- **§1.12 的探針在別的環境是否同樣有效**——只在本 repo 的開發環境實測過一種中間層。
  探針本身不綁技術棧，但「這五行足以偵測所有中間層的改寫」是推論，不是量測。
- **「第 2 層由專案填」是否真的會被填**——kit 只能規定，無法強制；
  §1.12 也明說「缺了本節在該專案就是空的」。這是已知的、刻意接受的缺口。
- **四支 SKILL 的落點是否最有效**——「掛在防幻覺機制底下」是設計判斷，
  沒有辦法證明 agent 讀到那裡時真的會執行自檢。

## 6. 未處理（下一步的候選，不在本工單）

- 本 repo 自己的 `CLAUDE.md` 尚未填第 2 層。它是 `is_seed_file()` 認定的種子檔，
  升級不覆蓋，要手動寫；且本輪對環境的變更（保護名單、版本）需重啟後探針通過才算生效，
  **在確認生效前寫進 CLAUDE.md 會變成一句沒被驗證過的斷言**。建議另開工單，
  待探針 A 實測通過後再落。
