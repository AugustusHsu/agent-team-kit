# 審查紀錄：PEV-DEV-AGENT-024

**工單**：[填上 §1.12 第 2 層](../tasks/PEV-DEV-AGENT-024.md)
**審查者**：code-reviewer
**日期**：2026-08-18
**結論**：✅ **APPROVED**

> ⚠️ **本輪開發與審查發生在同一個 context**（§2.3 明列的情況），比照 023 的處理：
> §1 每一條附可重跑的指令或檔案:行數，§4 集中列出僅人工判讀的部分。
>
> ✅ **本輪取證通道已通過 §1.12 探針**（2026-08-18 07:58，proxy 帶保護設定、三支探針全過），
> 下列 shell 輸出可作為審查證據——這是 §1.12 立節以來第一次能寫下這句話。

## 1. AC 逐條核對

| AC | 結果 | 證據（可重跑） |
|---|---|---|
| AC-01 標題明示第 2 層 | ✅ | `CLAUDE.md:49` `## 取證通道（\`team_protocol.md\` §1.12 第 2 層）` |
| AC-02 三件事寫齊 | ✅ | 中間層 `:53-60`／設定落點 `:62-66`／成本與回滾 `:77-80` |
| AC-03 生效條件兩條且缺一不可 | ✅ | `:65` proxy 必須重啟、`:66` 必須從新開的終端 |
| AC-04 驗證看 proxy 端 | ✅ | `:68-71`，含 `/proc/<pid>/environ` 指令與「空的不代表沒生效」 |
| AC-05 記下 fallback 坑 | ✅ | `:73-75` defer-until-idle 與靜默 fallback 到 8788／8789 |
| AC-06 不複製探針與判準 | ✅ | `grep -c 'FIDELITY' CLAUDE.md` → **0**；`grep -c '\| a \| b \|' CLAUDE.md` → **0**；只留「判準與探針在 §1.12，這裡不重複」 |
| AC-07 `kit/` 零改動 | ✅ | `git status --porcelain kit/ \| wc -l` → **0** |
| AC-08 測試與升級檢查 | ✅ | `uv run pytest` → **135 passed in 6.92s**；`./install.sh . --upgrade --dry-run` → 新增 0、更新 0、已是最新 60、保留 4、**待合併 0**；`precheck.py` exit=**0** |
| AC-09 BACKLOG 重生成 | ✅ | `BACKLOG.md:38` 出現 PEV-DEV-AGENT-024 |

## 2. 「待合併 0」為什麼是本工單最關鍵的一條

`CLAUDE.md` 是 `is_seed_file()` 種子檔，改它**不應該**在升級時產生任何 `.new`。
若第 2 層被誤寫進 `kit/`，dry-run 會出現「待合併」——**待合併 0 同時證明了
AC-07 與「沒有把本機環境寫死進出貨內容」**。這比單看 `git status` 強，
因為它是從安裝器的角度驗的。

## 3. 內容真確性：全部是實測，不是推論

| 寫進 CLAUDE.md 的斷言 | 來源 |
|---|---|
| 預設保護檔案讀取類、不保護 Bash | `headroom/config.py` `DEFAULT_EXCLUDE_TOOLS` 與其原始碼註解 |
| 未保真時五行塌成一行、散文全滅 | 2026-08-18 變更前實跑的基線 |
| 設定經環境變數繼承給 proxy | `wrap.py` 的 `os.environ.copy()` |
| 舊 proxy 被連著時不會被殺 | `wrap.py` 重啟分支「有 active session 就 defer」判斷 |
| 靜默 fallback 到 8788／8789 | `~/.headroom/clients/` 目錄時間戳：07:47→8789、07:49→8788、07:54→8787 |
| 有損壓縮只佔省下金額 1.2% | `/stats` 分層（有損 $79.61／總 $6,829.24） |

## 4. 僅人工判讀（無法客觀驗證）

- **「三件事寫齊」是語意判斷。** §1.12 的三項要求沒有機器可檢的形式，
  AC-02 的行號只能證明「有寫在那裡」，不能證明「寫得夠用」。
- **第 2 層是否對別人的環境也有用。** 本節寫的是這台機器的 headroom；
  若有人在沒跑 headroom 的環境開發本 repo，只能靠節內那句「本節不適用，但探針照跑」導引，
  **這條路徑未經實測**。
- **不列為 CI 檢查是延續 §1.12 的裁定**，代價是「專案沒填第 2 層」永遠不會被自動抓到——
  這個缺口在 023 的審查紀錄 §5 已記過，本工單只是把本 repo 這一格填上，沒有解決通則。

## 5. 未處理（不在本工單）

- headroom 0.34.0 → 0.35.0 升級（保真已在 0.34.0 達成，屬獨立維護動作）。
- kit 無法強制專案填第 2 層（設計缺口，見 023 審查紀錄 §5）。
