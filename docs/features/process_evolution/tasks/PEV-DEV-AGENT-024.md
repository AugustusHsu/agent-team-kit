# [Task ID: PEV-DEV-AGENT-024] 填上 §1.12 第 2 層：把本 repo 的取證通道設定寫進 CLAUDE.md

**🔗 依附母任務 (Parent Task ID):** —
**🏷️ 任務類型 (Task Type):** queue_agent
**👤 負責人 (Assignee):** devops-engineer
**🚥 任務狀態 (Status):** Done
**📅 建立時間 (Created):** 2026-08-18T08:06+08:00
**✅ 完成時間 (Closed):** 2026-08-18T08:20+08:00
**🔀 審查載體編號 (PR/MR):** —

## 1. 任務描述 (Description)

`PEV-DEV-AGENT-023`（Done）把「取證通道保真」立成 `team_protocol.md` **§1.12**，
並依 `DN-007` §3.2 做了兩層拆分：第 1 層寫規範、探針與判準（技術棧無關，kit 可定義），
**第 2 層留 hook 給專案填自己的通道設定**。§1.12 對第 2 層的原話是：

> 本守則只能定義到這裡——**它不知道你的專案跑在哪一套中間層上**。
> 每個專案要在自己的 `CLAUDE.md` 記下三件事，**缺了本節在該專案就是空的**。

本 repo 就是那個「缺了」的專案：023 只交付了第 1 層，本 repo 的 `CLAUDE.md` 至今沒有第 2 層，
**§1.12 在自己的 repo 裡是空的**。這在一個明文 dogfooding（用自己出貨的流程開發自己）
的專案裡是直接的自相矛盾。

**為什麼 023 當時不順手做完。** §1.12 自己寫著「**設定有寫進去不算數，探針通過才算數**」。
023 交付當下 proxy 尚未重啟、探針從未通過，此時寫下「保真已開啟、這樣設定即可」
會是一條**未經驗證的斷言**——正好違反本節自己立的判準。因此刻意押後，
並記在 `reviews/PEV-DEV-AGENT-023.md` §6。

**前提已於 2026-08-18 07:58 滿足。** proxy 重啟後帶著保護設定，三支探針從 shell 通道實跑通過：

| 探針 | 未保真時的基線 | 2026-08-18 07:58 實測 |
|---|---|---|
| 五行含表格＋前後散文 | 塌成 `[{"a":"1","b":"2"}]` 一行，兩句散文全滅 | **原樣返回，散文都在** |
| 16 行同構長輸出 | 第 13 行起消失 | **16 行全在，無壓縮標記** |
| `printf ... \| cat -A` | 換行 227 → 0 | **三行獨立，行尾 `$` 都在** |

現在寫下第 2 層，寫的是**已驗證的事實**而非設定意圖。

## 2. 規格：輸入與輸出 (Inputs & Outputs)

**輸入：** §1.12 第 2 層要求的三件事——
① 這條通道上有沒有會改寫輸出的中間層、是哪一套；
② 保真怎麼開（設定落點、生效條件，**特別是是否需要重啟**）；
③ 關掉它的成本與回滾方式。

**輸出：** 根目錄 `CLAUDE.md` 新增一節。`CLAUDE.md` 是 `install.sh` 的
`is_seed_file()` 種子檔（升級永不覆蓋），是本 repo 少數可直接編輯的安裝實例例外，
**不需要也不應該改 `kit/`**——第 2 層的定義就是「每個專案填自己的」，寫進 kit 等於
把本機環境寫死進出貨內容，違反 `017` 立下、`023` 續用的界線「出貨規範不得寫死特定產品名」。

## 3. 驗收標準 (Acceptance Criteria)

- [x] AC-01：`CLAUDE.md` 新增一節並在標題明示它是 **§1.12 第 2 層**，
      讀到的人能反查第 1 層規範。
- [x] AC-02：三件事**逐項寫齊**——中間層是哪一套（含它為何預設不保護 shell）、
      保真設定落點、成本與回滾。
- [x] AC-03：**生效條件寫明兩個且缺一不可**：proxy 必須重啟、必須從**新開的終端**啟動；
      這兩條都是 2026-08-18 實測踩到的，不是推論。
- [x] AC-04：寫明**驗證要看 proxy 端而非自己的 shell**——在既有終端
      `echo $HEADROOM_PROTECT_TOOL_RESULTS` 是空的不代表沒生效。
      這是本輪診斷時真實會誤判的點。
- [x] AC-05：記下重啟的坑：舊 proxy 被 session 連著時不會被殺（defer-until-idle），
      新 wrap **靜默 fallback 到別的 port**，於是以為換新了其實連在舊的上。
- [x] AC-06：**不複製 §1.12 的探針與判準**，只指路——同一條規則不得有兩份表述（§1.11）。
- [x] AC-07：只改根目錄 `CLAUDE.md` 與本工單／審查紀錄，**`kit/` 零改動**
      （`git diff --stat` 佐證）。
- [x] AC-08：`uv run pytest` 全綠；`./install.sh . --upgrade --dry-run` 待合併 0
      （證明沒有誤改安裝實例）。
- [x] AC-09：`BACKLOG.md` 重新生成，含本工單。

## 4. 人為補充與確認 (Human-in-the-loop)

- 使用者於 2026-08-18 明確指示「開 PEV-DEV-AGENT-024，把第 2 層寫進 CLAUDE.md」。
- 探針實測與 proxy 狀態診斷由使用者在同一輪要求並確認。

## 5. 範圍外 (Out of Scope)

- **不改 `kit/` 任何檔案。** 見 §2 的理由。
- **不寫成自動檢查。** §1.12 已裁定不列為 CI 檢查項（輸入不在 repo 內，
  CI runner 的 shell 必然保真，會永遠綠燈 → 假閘門）。本節同理，是給人／agent 讀的開工資訊。
- **不追 0.34.0 → 0.35.0 升級。** 保真在 0.34.0 已達成，升級是獨立的維護動作。
- **不改 `PEV-DEV-AGENT-017`／`023`**（§1.11：已結案工單不改也不引用其結論當現行規範）。

## 📝 Code Review 備註

第 2 層的內容全部來自 2026-08-18 的實測與原始碼取證，非推論：
保護名單來自 `headroom/config.py` 的 `DEFAULT_EXCLUDE_TOOLS` 及其原始碼註解；
繼承路徑來自 `wrap.py` 的 `os.environ.copy()`；
defer-until-idle 來自 `wrap.py` 重啟分支的「有 active session 就不重啟」判斷；
port fallback 由 `~/.headroom/clients/` 的目錄時間戳（07:47→8789、07:49→8788、07:54→8787）佐證。

### 📊 客觀指標

| 指標 | 值 |
|---|---|
| 改動檔案 | `CLAUDE.md`、本工單、審查紀錄、`BACKLOG.md` |
| `kit/` 改動 | 0 |
| 探針通過數 | 3／3 |
