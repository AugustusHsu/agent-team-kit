# [Task ID: PEV-DEV-AGENT-014] 冰箱清帳：移除已落地與前提消失的項目

**🔗 依附母任務 (Parent Task ID):** —
**🏷️ 任務類型 (Task Type):** queue_agent
**👤 負責人 (Assignee):** scrum-master
**🚥 任務狀態 (Status):** Done
**📅 建立時間 (Created):** 2026-08-17T21:40+08:00
**✅ 完成時間 (Closed):** 2026-08-17T21:55+08:00
**🔀 審查載體編號 (PR/MR):** —

## 1. 任務描述 (Description)

`BACKLOG.md` 的冰箱區塊自己寫著規則：

> 📌 **Icebox 只放「還沒決定」的事**——一旦決定（做或不做）就離開這裡。

現在有三列違反這條規則，而且**沒有任何自動檢查會抓到**（冰箱是人工維護區，
`scan_backlog.py` 只負責原樣保留它）：

| 列 | 為什麼該離開 |
|---|---|
| `scan_backlog.py` 的 Closed 欄位 regex 跨行誤配 | 已由 PEV-DEV-AGENT-012 修掉 |
| 生成檔不得含時間相依值 | 已由 PEV-DEV-AGENT-013 立規並落地 |
| 背景任務 worktree 隔離守衛 | **前提消失**：卡點寫的是「與本專案停用 worktree 相衝突」，而 worktree 禁用已於 2026-08-17 解除；且本 repo 根本沒有 `.claude/settings.json`（只有個人的、不進版控的 `settings.local.json`，其中沒有任何 worktree 設定）——本次實查 |

留著過期列的代價是規劃時會被誤導：下一輪讀冰箱的人會以為那三件事還沒決定。

## 2. 規格：輸入與輸出 (Inputs & Outputs)

**輸入**：`docs/development/BACKLOG.md` 的冰箱表格（9 列）。

**輸出**：6 列。三列移除，其中兩列的去向是工單（已 Done），一列是前提消失後直接作廢。
`headroom 壓縮工具輸出` 那列的「卡在哪」補上最新事證（本輪對話再度遇到）。

留下來的六列與其前提是否仍成立，逐列覆核：

| 項目 | 狀態 | 前提是否仍成立 |
|---|---|---|
| 負向對照自動化 | 待開工單 | ✅ 仍卡在 DN-007 定檢查落點 |
| precheck 腳本 | 可直接開工單 | ✅ 做法單一，但落點同樣在 CI，隨 DN-007 一起走 |
| 工單瘦身 | 待開 DN | ✅ 落點未定 |
| 展示層生成 | 待開 DN | ✅ 且冰箱自己註明「排最後」 |
| 審查與開發拆 context | 待判定 | ✅ 尚未裁定是工單還是 DN |
| headroom 壓縮工具輸出 | 待調查 | ✅ 事證再增加 |

## 3. 驗收標準 (Acceptance Criteria)

- [x] 冰箱表格剩 6 列，三列已移除
- [x] 兩列已落地項目的去向可追溯（本工單描述裡寫明對應的 Task ID）
- [x] `headroom` 那列的事證更新為現況
- [x] 重生 `BACKLOG.md` 後冰箱內容被原樣保留（驗證人工維護區沒被腳本吃掉）
- [x] 全套測試綠燈

## 4. 人為補充與確認 (Human-in-the-loop)

- 2026-08-17：本工單只動本 repo 的 `BACKLOG.md`（種子檔），不影響 kit 出貨內容。

## 📝 Code Review 備註

**審查人**：code-reviewer（2026-08-17）

### 客觀指標

| 指標 | 修正前 | 修正後 |
|---|---|---|
| 冰箱資料列 | 9 | **6** |
| 其中違反「決定了就離開」的列 | 3 | **0** |
| 重生 BACKLOG 後冰箱是否保留 | — | **保留**（實測：重生後 `負向對照自動化` 仍在、已移除的三列沒有復活） |

### 前提消失那列的查證過程

冰箱寫的卡點是「`.claude/settings.json` 的 `worktree.bgIsolation` 與本專案停用 worktree 相衝突」。
兩個前提都查了：

1. **停用 worktree** 已於 2026-08-17 解除（`CLAUDE.md` 現在寫的是「worktree 可以用」＋三條硬性要求）。
2. **`.claude/settings.json` 不存在**——目錄下只有 `settings.local.json`（個人設定、不進版控），
   且以 JSON 解析後確認其鍵只有 `env`／`permissions`／`hooks`，沒有任何 worktree 相關設定。

兩個前提都不成立，因此不是「改寫卡點」而是**整列作廢**。

### 為什麼冰箱需要人來清

冰箱是生成檔裡的人工維護區，`scan_backlog.py` 只負責原樣保留、不會判斷內容是否過期
（這也是 PEV-DEV-AGENT-013 §5.3 明文寫下的分工）。所以過期列不會有任何執行期訊號，
只能靠定期清帳——本工單就是那次清帳。

**結論**：通過。
